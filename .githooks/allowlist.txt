# Repo-tracked allowlist for the pre-commit guard (.githooks/pre-commit).
# One term per line, case-insensitive, plain substring match (no regex).
# '#' starts a comment; blank lines are ignored.
#
# Unlike ~/.claude/sensitive-terms.txt (a personal blocklist that lives
# outside any repo), this file is part of the repo: it exists to exempt
# known-legitimate, already-public content that would otherwise trip the
# guard's generic secret-like patterns on every commit that touches it.
# It only applies to those generic pattern checks, never to the personal
# blocklist.

# pikchr's own upstream copyright/attribution email, quoted verbatim in
# NOTICE and in the file headers of modules ported from pikchr.y.
drh@sqlite.org
